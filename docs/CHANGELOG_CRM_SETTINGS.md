# سجل التغييرات - تحديث نظام إدارة علاقات العملاء والإعدادات (v1.1.0)
التاريخ: 2025-12-09

## 🌟 ملخص التحديث
تم تحويل نظام ERP ليحتوي على ميزات CRM متقدمة، بما في ذلك سجل متابعة العملاء، المرفقات، إدارة الائتمان، ونظام إعدادات مركزي للتكامل مع WhatsApp.

---

## 🚀 الميزات الجديدة

### 1. إدارة العملاء المتقدمة (CRM)
- **سجل المتابعة (Activity Log):**
  - إمكانية تسجيل الملاحظات، المكالمات، والاجتماعات لكل عميل.
  - عرض السجل بتسلسل زمني (Timeline).
  - تمييز الأنشطة بالألوان حسب النوع.

- **المرفقات (Document Attachments):**
  - رفع ملفات (صور، عقود، هوية) في بروفايل العميل.
  - معرض صور مصغر للملفات المرفوعة.
  - تخزين الملفات محلياً في مجلد `uploads/documents`.

- **إدارة الائتمان (Credit Control):**
  - تحديد "حد ائتماني" (Credit Limit) لكل عميل.
  - منع إنشاء فواتير جديدة إذا تجاوز العميل الحد المسموح.
  - تحديث رصيد العميل تلقائياً عند البيع.

- **أدوات التواصل السريع:**
  - زر مباشر لفتح محادثة **WhatsApp** مع العميل.
  - زر مباشر لفتح موقع العميل على **Google Maps**.

### 2. نظام الإعدادات المركزي (Settings Module)
- **واجهة مستخدم جديدة:**
  - صفحة إعدادات بتصميم "Tabs" جانبي عمودي.
  - فصل الإعدادات إلى "عامة" و "تكاملات".

- **إعدادات التكامل (Integration):**
  - حفظ بيانات WhatsApp API (URL, Token, Sender).
  - تخزين البيانات بشكل آمن في قاعدة البيانات.

- **الإعدادات العامة:**
  - تغيير اسم الشركة (ينعكس فوراً على صفحة الدخول بالسايد بار الأيمن والأيسر).
  - **رفع شعار الشركة (Company Logo):**
    - حقل رفع صور (PNG/JPG).
    - يظهر الشعار في صفحة الدخول بدلاً من الأيقونة الافتراضية.
  - تغيير العملة الافتراضية للنظام (EGP, USD, SAR).

---

## 🛠️ التغييرات التقنية (Technical Changes)

### Backend (Go/Gin)
- **Services:**
  - `internal/services/notification_service.go`: خدمة إرسال الرسائل عبر WhatsApp API.
- **Handlers:**
  - `GetPublicSettings`: نقطة اتصال عامة لجلب بيانات الشركة والشعار بدون تسجيل دخول.
  - `UploadLogo`: نقطة اتصال لرفع شعار الشركة.

- **Integrations:**
  - ربط `CustomerUseCase` بـ `NotificationService` لإرسال تنبيهات واتساب عند إضافة نشاط من نوع `Alert`.

- **New Tables:**
  - `customer_activities`: لتخزين سجل المتابعة.
  - `customer_documents`: لتخزين بيانات الملفات.
  - `system_settings`: لتخزين إعدادات النظام (Key-Value Pair).
  - `notifications`: (بنية تحتية للإشعارات).

- **New Packages:**
  - `internal/repositories/settings_repository.go`
  - `internal/usecases/settings_usecase.go`
  - `internal/handlers/settings_handler.go`

- **Updates:**
  - `SalesUseCase`: إضافة منطق التحقق من الرصيد `Credit Limit`.
  - `main.go`: تفعيل خدمة الملفات الثابتة `/uploads`.

### Frontend (Vanilla JS)
- **New Pages:**
  - `pages-customer-profile.js`: إعادة تصميم كاملة مع Tabs.
  - `pages-settings.js`: صفحة الإعدادات الجديدة.

- **State Management:**
  - تحديث `main.js` لدعم التبديل بين التبويبات وحفظ الإعدادات.

---

## 📝 ملاحظات للمطورين
- **قاعدة البيانات:** يتم إنشاء الجداول تلقائياً عند التشغيل (`AutoMigrate`).
- **الملفات:** تأكد من وجود صلاحيات كتابة على مجلد `uploads`.
- **البيانات:** لا تقم بحذف ملف `erp.db` للحفاظ على الإعدادات.

---
**تم التطوير بواسطة: Antigravity Assistant**
